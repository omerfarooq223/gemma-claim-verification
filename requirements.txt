# Model versions used by the saved competition adapter.
transformers==5.10.1
peft==0.19.1
bitsandbytes==0.50.1
accelerate>=0.33.0
torch==2.8.0
torchvision==0.23.0
huggingface_hub>=1.5.0,<2
pyyaml>=6.0
# Gradio 4.44 requires the older Hub API; use a compatible UI release.
gradio==5.49.1
spaces==0.51.3
